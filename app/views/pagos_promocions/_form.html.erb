<%= form_with(model: pagos_promocion, local: true) do |form| %>
  <% if pagos_promocion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pagos_promocion.errors.count, "error") %> prohibited this pagos_promocion from being saved:</h2>

      <ul>
      <% pagos_promocion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id, id: :pagos_promocion_cliente_id %>
  </div>

  <div class="field">
    <%= form.label :publicidad_id %>
    <%= form.text_field :publicidad_id, id: :pagos_promocion_publicidad_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
