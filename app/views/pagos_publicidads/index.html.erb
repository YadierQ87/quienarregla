<p id="notice"><%= notice %></p>

<h1>Pagos Publicidads</h1>

<table>
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Promocioneventos</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pagos_publicidads.each do |pagos_publicidad| %>
      <tr>
        <td><%= pagos_publicidad.cliente %></td>
        <td><%= pagos_publicidad.promocionEventos %></td>
        <td><%= link_to 'Show', pagos_publicidad %></td>
        <td><%= link_to 'Edit', edit_pagos_publicidad_path(pagos_publicidad) %></td>
        <td><%= link_to 'Destroy', pagos_publicidad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pagos Publicidad', new_pagos_publicidad_path %>
